# Handoff — 2026-08-29, end of session

State of the world as measured, not as remembered. Checked against the working tree
and the running processes at the time of writing. The 2026-08-28 handoff's §1 items
were resolved (the model key lives in `config.env` now; rotation was deferred by the
owner to pre-launch, see §6) and its §6 order was executed to completion.

## 1. What is running

| | |
| --- | --- |
| Repo | `agentbox`, branch `main`, tree clean at the docs commits below |
| Origin | pushed this session; PR #13 (feat/dingtalk-cards) closes as merged with it |
| Tests | **945 pass, 0 fail**; `tsc` clean |
| Web | serving on `http://127.0.0.1:7777`; restarted at end of session onto current code |
| Channels | `feishu` (personal), `feishu-zongheng` (纵横's app, defaultAgent unset — pick Bob in settings), `dingtalk` — all connected |
| Box | `agentbox-box` healthy; `agentbox-acme` exited 2026-08-28, container kept (removal is an operator's call) |
| Model | MiniMax (China) MiniMax-M3, **no prompt caching** — which moots the prefix-stability audit until the provider changes |

**Restarting is no longer a ceremony.** Kill and relaunch immediately: the Feishu
socket retries on a narrated 5/15/30/60s backoff and logs `socket ready` when it is
actually up (R35, shipped). The old 60-second hand-count is retired.

## 2. What this session decided (the part reviews cannot redo)

- **The domain model is docs/22, normative**, six review rounds to a fixed point:
  two gates (installation role, box membership), a box is a set of users
  (1 = private, all = shared, subset = department-shared), doors route and never
  authorize, workers are uniform inside a box. Review history:
  `docs/reviews/2026-08-29-domain-model.md`.
- **The owner's ruling closed the private-box question**: a private box is a
  special-made box one person uses and others cannot even see; no department
  boxes as their own concept; **docs/22 items 5–6 and docs/18 steps 4–8 are
  deferred until a multiuser mechanism exists**. Today every box is shared and
  says so. Do not re-open before multiuser.
- **The v4 identity-box reviews are written up**
  (`docs/reviews/2026-08-28-identity-box-v4.md`) and corrected into docs/18;
  the known-false sentences are gone.

## 3. What shipped (both days, condensed)

Channel records with immutable ids and pinned incarnations; identity links carrying
their incarnation with CAS on every writer; ChatRefs failing closed (deliveries,
conversations, and the pushToChat backstop for everything unstamped); Feishu and
DingTalk prefix-parameterized so second doors are config rows; per-door
`defaultAgent`; the roster verb (团队/成员/谁在) and the wrong-`@` correction from
the same list; settings as the full door console (add/edit/remove, credentials into
config env by the one envBase rule, hot-start — a new door opens without a restart);
box identity (`box.json`, `boxId` on every agent, immutable by shape); `classifyBox`
never keyed by URL; the noVNC takeover page labelled; provisioner refusal degrading
to "no box"; the R35 connect retry; scripts/feishu-enable-* parameterized by door id
plus the new group-messages one.

## 4. Live second-door notes (feishu-zongheng)

- New doors mint new identities: first message knocks, approve/bind links it — the
  CAS path. Enterprise switches change DingTalk ids too (measured, twice).
- Group audibility is a **vendor grant, not a connection**: run
  `node scripts/feishu-enable-group-messages.mjs feishu-zongheng` (scan-confirm) if
  groups are still silent; `feishu-enable-cards.mjs feishu-zongheng` for buttons.
- Document reading stays on the grandfathered door's credential — a box capability,
  not a per-door one (docs/22 §3).

## 5. Open, ranked

1. **Involute as kanban front end** — asked 2026-08-28, still unanswered.
2. **Telegram prefix parameterization** — the last hardcoded namespace; build when
   someone wants a second telegram door.
3. **Namespace replacement mechanism** — item 2's last piece (operator flow +
   proven-current bypass; lifts the incarnation≠1 startup refusal). Needed the
   first time an existing door's app/tenant is actually replaced, not before.
4. **R36 hot-loadable extensions** — direction recorded with Pi/cordis references;
   deliberately unstarted (security face; the cheap value already shipped).
5. Old backlog, none urgent (workId/kind turned out to be **already shipped** —
   the roadmap entry was stale and now says so): semantic cursor, AT-SPI click
   verification, officecli+OCR toolbox, team-box screen-capture honesty (becomes
   load-bearing at multiuser, same gate as items 5–6).
6. "R1's brief names Rex" resolved by time — no Rex reference survives in any
   memory or skill file; nothing to fix.

## 6. The pre-launch security list (owner deferred these to launch, on record)

- Rotate `MINIMAX_CODE_CN_API_KEY` (in a transcript, 2026-08-28) and the
  feishu-zongheng app secret (pasted in a chat, 2026-08-29).
- The web has **no UI token** — anything reaching :7777 drives the agents; the log
  says so at every start.
- docs/15's decision on where channel credentials live long-term (they sit in
  `config.json`'s env map today, 0600).

---

## Late-session update (end of the second working stretch)

Everything above stands; the afternoon and evening added three finished lines:

**R37 meeting presence — concluded.** The browser path was proven end-to-end in a
real meeting (join, screen share, watched live) and then terminated by vendor risk
control — confirmed by hand, no agent involved:「由于安全原因会议中止」. Not to be
engineered around; the official `bots/join` is the sanctioned route and is blocked
solely on a tenant admin switch (121016; ticket ammunition in the roadmap). The
「入会 <9位号>」verb, the remote-control option and the join scripts all remain.

**Context/memory — the whole line, closed.** docs/23 (root cause: entry triggers),
docs/24 v3 (three review rounds), all six P0 items, two verification rounds (Codex
+ self-driven Grok via herdr), and both P1 debts: Rememberer writes serialize per
agent, and CompactionGuard pauses a conversation whose compactions demonstrably do
not help. 961 tests.

**R31 spend view — discovered shipped.** Day totals, 104 tasks costed, drill-downs,
rate caveats. Open half is config: fill `rates` in config.json with real prices.

**Herdr unlocked**: this agent can now prompt and read the Grok pane directly
(`herdr agent prompt wC:p5 …`); review rounds no longer need the operator as a
relay. Names may not resolve — use pane ids.

**Next chartered workstream** (owner's directive): hands-on exploration of
agent/box built-in skills, ACP, and coding-agent delegation — the workbuddy preset
suite, the doubaowork companion crew (built-in skills + personas), and driving
opencode/pi/ACP from an agent including interruption semantics. Recon starts in
docs/25.
