# Handoff — 2026-08-30, end of session

Measured against the tree, the running processes, and the box at the time of
writing. Yesterday's handoff stands for everything it covers; today added the
whole docs/25 program and the catalog.

## 1. What is running

| | |
| --- | --- |
| Repo | `agentbox`, branch `main` at the docs commit below; origin in sync |
| Tests | **970 pass, 0 fail**; `tsc` clean |
| Web | `http://127.0.0.1:7777`, restarted onto current code with the relay env live |
| Channels | `feishu`, `feishu-zongheng`, `dingtalk` — connected |
| Box | rebuilt ×3 today; **opencode 1.18.25 pinned in the image** (scs browser login lost with the recreates — accepted, the meeting path is dead) |
| Roster | Ada/Bob/Vic + **Iris 研究员 / Mia 秘书 / Enzo 工程师** (E4/E5 crew, live) |
| Relay | `AGENTBOX_RELAY_URL/TOKEN/MODEL` in config env → MiniMax degenerate relay; the `missing: delegated engines` absence is gone |

## 2. The docs/25 program — all five experiments run (run logs in `research/workbuddy/`, local-only)

- **E1**: opencode pinned into the image. Rebuild is
  `OPENCODE_VERSION=1.18.25 agentbox box build` — without the env var the
  engine layer silently stays off (both build paths honor it now). Three
  ownership/shadowing bugs fixed at the source of truth. Measured: opencode
  ignores `*_BASE_URL` env; `--auto` or every tool call is silently rejected;
  model travels as `-m` via `AGENTBOX_RELAY_MODEL`.
- **E2**: Delegate end-to-end through Bob, four probes. 停 is turn-level at
  round boundaries (60s wait chunks set the granularity) and never kills the
  job; `Jobs kill` is the job-level act, log preserved; steering queues
  between chunks; one-shot runs cannot be steered mid-flight.
- **E3**: `opencode serve` session mode measured: token-level SSE, mid-flight
  steering that landed in code, **33ms abort**, context survives abort. The
  supervised-delegation upgrade is one flag away; deferred behind docs/13.
- **E4+E5**: the crew as pure data via `POST /api/agents`; three one-sentence
  golden tasks passed in parallel with externally verified artifacts. Personas
  shaped judgment, not just tone (Enzo chose DIY over Delegate for a two-line
  fix).

## 3. PR #14 reviewed post-merge (the catalog) — verdict: sound, docs gap fixed

Grok's catalog landed: seven experts + two crews as persona files plus
vendored skill-hub packages, seeded by the marker mechanism; tool tiers with
`intersectTools` privilege clamping; installs via web chips / `CreateAgent
from` / `GET /api/catalog`; fresh installs seed Ada/Rex/Ops/Vera (existing
rosters untouched). 970 tests, tsc clean, provenance recorded in
`catalog-data/SOURCE.md`. The one defect was **zero docs** — fixed today:
docs/25 now carries the catalog section and how it relates to the E4/E5
manifests. Catalog rows still lack golden acceptance tasks; the manifests in
`research/workbuddy/` show the shape.

## 4. Intermediate products — none left unmerged

- The dirty `agentbox-dingtalk` worktree diff was a **stale local draft** of
  fixes already in main as fd47e81 (confirmed by Grok, then verified against
  main). Discarded; worktree fast-forwarded to main and pushed.
- `feat/dingtalk-cards`, `feat/dingtalk-parity`: fully merged, no open PRs.

## 5. Open, ranked

1. **Crew week-of-use verdict** — Iris/Mia/Enzo are live; "would the owner
   keep them" is the owner's week, started 2026-08-30.
2. **Golden tasks onto catalog rows** — the acceptance ritual for engine or
   provider changes; shape exists in the E4/E5 manifests.
3. **Session face for supervised delegation** (E3's finding) — gates on
   docs/13; wire abort to 停 and `/event` to the web page when built.
4. **飞书办公 bundle** — blocked on write-side Feishu tools (sheets/bitables)
   or an in-box credential decision.
5. Carried from yesterday: Involute kanban answer, telegram prefix param,
   namespace replacement, R36 hot extensions, real `rates` in config.

## 6. Pre-launch security list (grows by one)

Yesterday's three items stand (rotate the MiniMax key + zongheng secret; UI
token; docs/15 credential home). Added: **E1's degenerate relay puts the
MiniMax key in the box's process env** for delegated runs — quarantined to
this test tenancy, remove when a real relay exists.
